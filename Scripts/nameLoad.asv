function [file] = nameLoad(file)
file.name = nan(file.num,1);
file.file_name = nan(file.num,1);
zi = 1;
for i = 1:file.num
    file.file_name(i) = convertCharsToStrings(file.meta(i).name);

    zi = zi + 1;
end
end